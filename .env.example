# Variables de entorno de ejemplo. Copiar a .env.local y completar valores reales.
# NUNCA versionar .env.local ni exponer esta clave con prefijo NEXT_PUBLIC_.

# API key de Google Cloud con "Google Drive API" habilitada (solo lectura de archivos publicos/compartidos).
GOOGLE_DRIVE_API_KEY=
